/*
 * DrinkService Messages
 *
 * This contains all the text for the DrinkService component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DrinkService';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DrinkService component!',
  },
});
